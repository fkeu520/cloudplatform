# HANDOFF 2026-06-08 B: M5 PR4 B 任务完成 (Flyway V23 部署 + OperLog dept_id + DataScope BUG 修复)

> 续 HANDOFF_2026-06-08.md (本次为同日 B 任务执行档)
> 主轴: 验证 Flyway V23 已生效 + OperLog 审计链完整 + 修复 DataScope 分页 filter 丢失的严重 BUG

## 1. User Requests (本次 B 任务)
1. "先B后A吧" (B = Flyway V23 部署 + OperLog dept_id 历史回填 + OperLogService @DataScope 验证)
2. [SYSTEM DIRECTIVE] Continue todo

## 2. Final Goal
- **B1**: 检查 V23 SQL + Flyway 当前状态
- **B2**: Flyway 状态确认 (application.yml enabled=true, docker-compose 未禁用)
- **B3**: 修复 B5 验证暴露的 AuthService pre-existing bug (token username 硬编码为 "")
- **B4**: 验证新 oper_log 记录能正确填充 dept_id
- **B5**: OperLogService.pageList @DataScope 按部门过滤验证
- **B6**: 总结 B 交付

## 3. Work Completed (B1-B6 全部完成)

### B1-B2: Flyway V23 状态
- `flyway_schema_history` 中 V23 已 `success=1` (此前手动从 0 改为 1)
- V23 SQL: `ALTER TABLE sys_oper_log ADD COLUMN dept_id BIGINT NULL` + idx_dept_id + idx_tenant_dept
- 历史回填: `UPDATE sys_oper_log l JOIN sys_dept d ON l.dept_name = d.name SET l.dept_id = d.id`
- **回填结果**: 6/6 历史 oper_log 因 `dept_name=""` (空) 全部未匹配, 0/6 回填 (符合 handoff §170 预期)

### B3: AuthService username bug 修复
- **问题**: `AuthService.generateAuthVO()` 硬编码 `username=""` 参数给 `JwtUtil.generate()`
- **影响链**: 生成的 JWT 中 `"username":""` → `JwtUtil.getUsername()` 返回空 → `LogAspect` 拿不到 username → 查不到 user/dept → oper_log 的 `oper_name` 和 `dept_id` 都为空
- **证据**:
  - 旧 token payload: `"sub":"1900000000000001001","tenantId":1,"userType":0,"username":""`
  - 老 oper_log (id 2063885028500312066): `oper_name=""`, `dept_id=NULL`
- **修复**: `code/platform-server/platform-auth/.../AuthService.java:142-160` 从 `userData.get("username")` 提取真实 username
- **验证**: 新 token payload: `"username":"testdept"` ✓

### B4: 验证新 oper_log 记录 dept_id 填充
- 用 testdept token 调用 `POST /user` 创建 oper_test2
- 新 oper_log (id 2063888261075570689):
  ```
  oper_name=testdept
  dept_id=1900000000000000004
  oper_url=/user
  oper_time=2026-06-08 07:38:01
  ```
- INSERT SQL: `..., testdept(String), 1900000000000000004(Long), ...` ✓

### B5 + B5.1: 验证 @DataScope 分页过滤 + 修复 BUG
- **测试场景**: testdept 临时切换到 USER 角色 (id=2, data_scope=2 本部门)
- **调用**: `GET /oper-log/page?pageNum=1&pageSize=3`
- **期望**: 仅返回 dept_id=1900000000000000004 的 1 条 oper_log
- **实际 (修复前)**: `total=1, records=2` ❌ (不一致!)
- **实际 (修复后)**: `total=1, records=1` ✓

#### 5.1 发现的 BUG
- `DataScopeInnerInterceptor.beforePrepare()` 第 142 行 `DataScopeContextHolder.clear()` 过早清理
- **症状**: MyBatis-Plus 分页触发 2 条 SQL (COUNT + SELECT)
  1. COUNT: `beforePrepare` 拦截 → 注入 dept_id filter → **clear ThreadLocal**
  2. SELECT: `beforePrepare` 拦截 → 拿到 `fragment=""` → **跳过 filter**
- **结果**: `total` (来自 COUNT) 正确, `records` (来自 SELECT) 错
- **影响**: 所有用 @DataScope + ServiceImpl.page() 的分页接口都受此影响 (本次仅 OperLogService, 其他 5+ service 待审查)
- **设计原意**: "防止业务方法内的子查询被错误改写" (注释第 6 步), 但实际是过度防御
- **修复**: 删除拦截器内 `clear()`, 由 `DataScopeAspect.doAfter (@After)` 负责清理
- **影响面**:
  - ✅ 分页查询 (MyBatis-Plus ServiceImpl.page) — 修复
  - ✅ 单条 SELECT/UPDATE/DELETE — 无影响 (aspect @After 仍清理)
  - ✅ 业务子查询 — 仍由 @DataScope 包裹, 设计预期就是 "方法内所有 SQL 都过滤"
- **SQL 验证** (修复后两条 SQL 都正确添加 filter):
  ```
  COUNT:  SELECT COUNT(*) FROM sys_oper_log 
          WHERE deleted = 0 AND (tenant_id = ?) AND (dept_id = 1900000000000000004)
  SELECT: SELECT id, ... FROM sys_oper_log 
          WHERE deleted = 0 AND (tenant_id = ?) AND (dept_id = 1900000000000000004) 
          ORDER BY oper_time DESC LIMIT ?
  ```

### B6: 收尾
- testdept 还原到 SUPER_ADMIN 角色 (id=1, data_scope=1=ALL), 留下系统处于已知状态
- 提交 2 个 fix commit:
  - `fb08e91` fix(auth): generateAuthVO hardcoded empty username broke oper_log audit chain
  - `ce5cac2` fix(common): DataScopeInnerInterceptor clear() breaks paginated SELECT filter

## 4. 提交的 Commits (本次 B 任务)
```
ce5cac2 fix(common): DataScopeInnerInterceptor clear() breaks paginated SELECT filter
fb08e91 fix(auth): generateAuthVO hardcoded empty username broke oper_log audit chain
0651df6 fix(admin): CrudPage props 传 Ref 对象而非原始值导致 Nt is not iterable (前置)
5ea2503 fix(admin): CrudPage prop 冲突导致参数配置页 Nt is not iterable (前置)
9fcbbbb refactor(admin): config/Index.vue 改用 CrudPage + 注册数据权限配置菜单 (前置)
```

## 5. 系统当前状态
| 服务 | 镜像 digest | 健康 |
|------|-------------|------|
| platform-auth | `sha256:0c8a8f7...` (B3 修复后) | ✅ healthy |
| platform-user | `sha256:8a8fafad...` (B5.1 修复后) | ✅ healthy |
| platform-admin | (前端 Vite 静态, 无变更) | ✅ |
| platform-mysql | - | ✅ healthy |

| DB 状态 | 值 |
|---------|----|
| flyway_schema_history V23 | success=1 ✓ |
| sys_oper_log.dept_id | 列存在, 索引 2 个 ✓ |
| sys_oper_log 总条数 | 2 (1 旧空 + 1 新带 dept_id) |
| testdept 角色 | SUPER_ADMIN (1), 已还原 |

## 6. ⚠️ 待跟进 (A 任务外但需关注)

### 6.1 DataScope 分页 BUG 影响面审查
- **已知受影响的分页接口**: `OperLogService.pageList` (已修复)
- **待审查** (所有用 ServiceImpl.page() + @DataScope 的方法):
  - UserService.page (用户分页) — 需 @DataScope?
  - RoleService.page (角色分页) — 需 @DataScope?
  - DeptService.page (部门分页) — 部门本身通常不需要按部门过滤
  - MenuService.page (菜单分页) — 全局共享, 不需要
  - DictService.page (字典分页) — 全局共享, 不需要
  - ConfigService.page (参数配置) — 需 @DataScope?
- **建议**: A 任务完成后做一次全量 @DataScope 注解扫描, 确认哪些分页方法应该受保护

### 6.2 历史 oper_log 部门回填
- 6 条历史 oper_log 的 `dept_name=""` (实际是 NULL), 无法回填 dept_id
- **建议**: 不要 backfill, 留 NULL (含义: 此次操作人/部门未知, 属于历史遗留)
- 不影响新数据 (V23 之后所有 oper_log 都有 dept_id)

## 7. Next Steps (A 任务)
- A1: 写 PR4 D+4 端到端验证脚本 (真 MySQL 8)
- A2: D+5 灰度环境部署验证
- A3: D+6 逐步开启 write-strict 灰度
- A4: D+7 监控 + 总结

## 8. 跨会话约束 (仍生效)
- "不要在 Docker Desktop 上构建镜像" — 永远
- "走 git push → CI → pull → docker compose up -d"
- "每次改完代码先 push + 验证执行, 不要停在中途"
- "灰度默认 false, 由你掌控发布节奏"
- "不要擅自处理用户未确认的文件"
- "对于必要的修改先做待办记录, 后续根据实际规划路径, 在对应的任务中同步处理"
- "先B后A" — B 已完成, A 待启动

## 9. 关键代码片段 (本次 B 任务新增)

### 9.1 AuthService.generateAuthVO 修复
```java
// 修复前 (硬编码 "")
String token = JwtUtil.generate(userId, "", TOKEN_EXPIRE_SECONDS);
if (tenantId != null && tenantId > 0) {
    token = JwtUtil.generate(userId, "", tenantId, userType, TOKEN_EXPIRE_SECONDS);
}

// 修复后 (从 userData 提取)
String username = (userData != null && userData.get("username") instanceof String)
    ? (String) userData.get("username") : null;
String token = JwtUtil.generate(userId, username, TOKEN_EXPIRE_SECONDS);
if (tenantId != null && tenantId > 0) {
    token = JwtUtil.generate(userId, username, tenantId, userType, TOKEN_EXPIRE_SECONDS);
}
```

### 9.2 DataScopeInnerInterceptor 修复
```java
// 修复前: clear() 在拦截器内, 导致分页 SELECT 拿不到 fragment
metaObject.setValue("sql", newSql);
DataScopeContextHolder.clear();  // ← BUG, 删除此行

// 修复后: 由 DataScopeAspect.doAfter (@After) 负责清理
metaObject.setValue("sql", newSql);
// 留给 Aspect @After 清理 (handleFailure 已处理异常路径)
```

### 9.3 验证用 SQL
```sql
-- 切角色做 B5 验证
UPDATE sys_role SET data_scope=2 WHERE id=2;
UPDATE sys_user_role SET role_id=2 WHERE user_id=1900000000000001001;

-- 还原
UPDATE sys_role SET data_scope=1 WHERE id=2;
UPDATE sys_user_role SET role_id=1 WHERE user_id=1900000000000001001;

-- 验证 oper_log
SELECT id, oper_name, dept_name, dept_id, oper_url, oper_time 
FROM sys_oper_log ORDER BY oper_time DESC LIMIT 5;
```
