# P0-2 数据权限 data_scope · M5 实施子任务

**版本**: v1.0
**日期**: 2026-06-04
**配套**: [主规划 v1.0 §5.2](中台建设中长期规划.md) · [P0-1-M4-完成度审计.md](P0-1-M4-完成度审计.md)
**状态**: 🔵 准备中 (今日启动)

---

## 一、目标

为角色增加**行级数据权限**，区分同角色不同数据范围（如"销售"角色，经理看本部门，员工看本人）。

**5 种 data_scope**:

| 值 | 含义 | SQL 效果 |
|----|------|---------|
| 1 | 全部 | 不加条件 |
| 2 | 本部门 | `WHERE dept_id = ?` |
| 3 | 本部门及下级 | `WHERE dept_id IN (子部门)` |
| 4 | 本人 | `WHERE user_id = ?` |
| 5 | 自定义 | `WHERE dept_id IN (custom_dept_ids)` |

**多角色合并策略** (待用户拍板): **取最严格** (scope 值大者) / **取并集** / **主角色优先**

---

## 二、当前现状

### 2.1 已具备

- ✅ `DataScope.java` 注解已存在 (`com.cloudhub.platform.common.annotation.DataScope`)
- ✅ `DataScopeAspect.java` 已存在 (但 22 行，仅是个桩)
- ✅ `sys_role` 表已存在 (V1)
- ✅ 多租户拦截器工作正常 (P0-1 已完成)
- ✅ TenantContextHolder 提供 userId / tenantId ThreadLocal

### 2.2 缺失

- ❌ `sys_role.data_scope` 字段
- ❌ `sys_role.custom_dept_ids` 字段
- ❌ DataScopeAspect 真实 SQL 拼接逻辑
- ❌ DataScope 集成测试用例
- ❌ MyBatis DataScopeInnerInterceptor (新增，可选)

---

## 三、实施步骤

### 3.1 V22 migration (DB schema)

新增 `sys_role.data_scope` (TINYINT) + `custom_dept_ids` (VARCHAR 1000) 字段。

```sql
-- V22: data_scope + custom_dept_ids
ALTER TABLE sys_role ADD COLUMN data_scope TINYINT DEFAULT 1
    COMMENT '数据范围 1=全部 2=本部门 3=本部门及下级 4=本人 5=自定义';
ALTER TABLE sys_role ADD COLUMN custom_dept_ids VARCHAR(1000) DEFAULT NULL
    COMMENT '自定义部门ID列表 (data_scope=5 时使用, 逗号分隔)';
```

### 3.2 @DataScope 注解扩展

**当前**: 已存在
**建议扩展**: 加 `deptAlias` / `userAlias` 参数 (支持复杂 join 场景)

```java
@Retention(RetentionPolicy.RUNTIME)
@Target(ElementType.METHOD)
public @interface DataScope {
    /**
     * 部门表别名 (如 "d", "dept")
     * SQL: WHERE {deptAlias}.id = ?
     */
    String deptAlias() default "";

    /**
     * 用户表别名 (如 "u", "user")
     * SQL: WHERE {userAlias}.id = ?
     */
    String userAlias() default "";
}
```

### 3.3 DataScopeAspect 完整实现 (重写当前 22 行桩)

**核心逻辑**:

```java
@Before("@annotation(dataScope)")
public void doBefore(JoinPoint point, DataScope dataScope) {
    Long userId = TenantContextHolder.getUserId();
    if (userId == null) return;

    // 1. 查用户的所有 role + data_scope + dept_id
    List<UserRoleDataScope> scopes = userRoleMapper.selectDataScopesByUserId(userId);

    // 2. 合并 data_scope (取最严格)
    int maxScope = scopes.stream()
        .map(UserRoleDataScope::getDataScope)
        .filter(Objects::nonNull)
        .max(Integer::compare)
        .orElse(1);

    // 3. 拼 SQL 条件, 写入 ThreadLocal
    String sqlFragment = buildSqlFragment(maxScope, scopes, dataScope);
    DataScopeContextHolder.set(sqlFragment);
}

private String buildSqlFragment(int scope, List<UserRoleDataScope> scopes, DataScope dataScope) {
    switch (scope) {
        case 1: return "";  // 全部, 不加条件
        case 2: return String.format(" AND %s.dept_id = %d",
            dataScope.deptAlias(), currentUserDeptId);
        case 3: return String.format(" AND %s.dept_id IN (%s)",
            dataScope.deptAlias(), childDeptIds);  // 子部门递归
        case 4: return String.format(" AND %s.id = %d",
            dataScope.userAlias(), currentUserId);
        case 5: return String.format(" AND %s.dept_id IN (%s)",
            dataScope.deptAlias(), customDeptIds);  // custom_dept_ids
        default: return "";
    }
}
```

### 3.4 MyBatis DataScopeInnerInterceptor (新增, 复杂)

**复用 P0-1 TenantLineInnerInterceptor 模式**, 新增 `DataScopeInnerInterceptor`:
- 读 `DataScopeContextHolder` 拿到 SQL 片段
- 拼到 SQL 末尾

**实现路径**:
```java
interceptor.addInnerInterceptor(new TenantLineInnerInterceptor(...));
interceptor.addInnerInterceptor(new DataScopeInnerInterceptor(...));
interceptor.addInnerInterceptor(new PaginationInnerInterceptor(...));
// ⚠️ 顺序: TenantLine 在前 (过滤), DataScope 在中, Pagination 在后
```

### 3.5 @DataScope 注解使用示例

```java
@Service
public class UserService {
    @DataScope(deptAlias = "u", userAlias = "u")
    public List<User> listByRole() {
        return userMapper.selectList(...);
    }
}
```

**自动生成的 SQL**:
```sql
-- scope=2 (本部门)
SELECT ... FROM sys_user u WHERE u.tenant_id = 1 AND u.dept_id = 100
```

### 3.6 集成测试 (DataScopeIsolationTest)

**3 个 TC** (M5 起步版):

| TC | 场景 | 期望 |
|----|------|------|
| TC-DS-01 | HR 经理 A (scope=2) 看本部门 | 看不到 B 部门员工 |
| TC-DS-02 | 销售员 A (scope=4) 看本人 | 只看到自己 |
| TC-DS-03 | 普通员工 A (scope=1) 看全部 | 看到本租户所有 |

---

## 四、决策日志 (待用户拍板)

### 4.1 多角色 data_scope 合并策略

| 策略 | 实现 | 优点 | 缺点 |
|------|------|------|------|
| **A. 取最严格** (scope 值大者) | `max(dataScope)` | 安全 (越严格越限) | 误限 (用户应有权限也被限) |
| **B. 取并集** (OR 合并) | `dept_id IN (union)` | 灵活 | 复杂 SQL |
| **C. 主角色优先** | `role.isPrimary = true` | 简单 | 需新增字段 |

**推荐 A** (与 L2 优化"安全优先"一致)。

### 4.2 dept 子树查询 (scope=3)

| 方案 | 实现 | 性能 |
|------|------|------|
| **A. 递归 CTE** (MySQL 8.0+) | `WITH RECURSIVE` | 中 |
| **B. 应用层递归** | Java 递归查 sys_dept | 慢 (N+1) |
| **C. 物化路径** (mpath) | 存 `path = "1/2/3/"`, LIKE 查询 | 快 |

**推荐 A** (MySQL 8.0 已支持, 单 SQL 一次返回)

### 4.3 是否复用 TenantLineInnerInterceptor 模式

**推荐复用**: 同一拦截器链, 模式一致, 维护成本低。

---

## 五、风险与回滚

### 5.1 风险

| 风险 | 缓解 |
|------|------|
| V22 migration 失败 | ALTER TABLE 幂等 (加 DEFAULT 1 兼容老数据) |
| DataScope 漏注解 | 强制 code review: 所有 list 方法必须有 @DataScope |
| 跨服务调用 dept_id 丢失 | 跨服务调用链 dept_id 显式传 |
| 多角色策略导致误限 | 业务测试覆盖, 默认 scope=1 (最宽松) |

### 5.2 回滚

```sql
-- 回滚 V22
ALTER TABLE sys_role DROP COLUMN data_scope;
ALTER TABLE sys_role DROP COLUMN custom_dept_ids;
```

**代码回滚**: git revert (单 commit)
**运行时回滚**: DataScopeAspect 整体加 `@ConditionalOnProperty(platform.data-scope.enabled, matchIfMissing = true)`, 关闭走 v3.1 行为。

---

## 六、变更历史

| 版本 | 日期 | 变更 |
|------|------|------|
| v1.0 | 2026-06-04 | 初版: 实施子任务 + 决策点 + 风险评估 |

---

## 七、今日完成 + 明日 TODO

### 今日已完成 (2026-06-04)

- [x] M5 实施子任务文档 v1.0 (本文档)
- [x] V22 migration (新文件)
- [x] @DataScope 注解扩展 (deptAlias / userAlias)
- [x] DataScopeAspect 重写 (从 22 行桩到完整实现)
- [x] DataScopeContextHolder 新建 (ThreadLocal)
- [x] DataScope 集成测试 (1-2 TC 起步版)

### 明日 TODO (2026-06-05)

- [ ] MyBatis DataScopeInnerInterceptor (复杂, 留到 M5-M2)
- [ ] 跨 dept 子树递归 (CTE) 实现
- [ ] 业务层全量加 @DataScope 注解 (涉及 ~10 个 Mapper)
- [ ] 拍板多角色合并策略 (A/B/C)

---

## 八、PR 拆分 (v1.1, 2026-06-05 拍板)

**触发**: 决策记录 v1.1 修订, 缺口 #15-#19 5 项 P0 必修拆 4 PR
**总工作量**: 8-10 天 (2 周)
**灰度开关**: `platform.data-scope.upgrade.enabled` (默认 false, 复用 v7.1 通用开关)

### PR1: dept 树 CTE 改造 + tenant 过滤 (缺口 #19)

| 项 | 内容 |
|----|------|
| 涉及缺口 | #19 (scope=3 dept 树无租户过滤) |
| 涉及决策 | 决策 2 修订 (A CTE 修正实施) |
| 工作量 | 1 天 |
| 改动文件 | `UserDataScopeProviderImpl.java` (双实现 + 灰度分支) + `DeptMapper.java` (1 个 CTE 查询) + 5 个 yml (灰度开关) + 1 个 CI 脚本 |
| 风险 | 低 (1 个 mapper 方法, 灰度开关可控) |
| 验证 TC | 3 个 (跨租户无 dept_id 泄漏 / 含本部门 / admin 无租户返回全量) |
| 灰度 | `platform.data-scope.upgrade.enabled=true` 启用 CTE; false 走应用层递归 (v7.0 行为) |

**完整准备文档**: [doc/项目进度.md v7.1 §七](../项目进度.md)
- §7.1 H2 mock 桩设计 (3 候选方案 + 推荐 A+C 组合 + SQL 模板)
- §7.2 灰度开关 yml 配置 (5 服务同步 + UserDataScopeProviderImpl 灰度分支)
- §7.3 commit message 模板 (Conventional Commits 格式)
- §7.4 启动 checklist (10 步)

**实施步骤** (按准备文档 §7.4 推进):
1. `DeptMapper` 新增 `selectChildDeptIdsByCte(rootDeptId, tenantId)` 方法, SQL 模板见 v7.1 §7.1
2. `UserDataScopeProviderImpl.collectChildDeptIds()` 改写为双实现:
   - 灰度开关 true → 调 CTE 方法 (新)
   - 灰度开关 false → 调原 DFS 方法 (保留作 fallback)
3. 5 个 yml 加灰度开关 (模板见 v7.1 §7.2)
4. CI 验证脚本 `check-data-scope-upgrade-toggle.sh`
5. H2 集成测试 (先试 A 方案, 不通 fallback B)
6. docker exec 真 MySQL 8 跑 CTE 端到端
7. git commit message 用 v7.1 §7.3 模板

### PR2: 业务层 8 个 @DataScope 注解补全 (缺口 #17)

| 项 | 内容 |
|----|------|
| 涉及缺口 | #17 (业务层 @DataScope 覆盖率 2/10) |
| 工作量 | 1-2 天 |
| 改动文件 | RoleService / DeptService / MenuService / PostService / OrgService / DictService / OperLogService / TenantAppService (8 个) |
| 风险 | 中 (业务可见性变化, 业务方需培训) |
| 验证 TC | 8 个 (每 Service 1 个 list 受限 TC) |
| 灰度 | `platform.data-scope.upgrade.enabled=true` 启用注解; false 注解不生效 |

**实施步骤**:
1. 8 个 Service.list/page 方法加 `@DataScope(deptAlias="...", userAlias="...")`
2. 字段名核对: Role/Dept/Menu/Post/Org/Dict/OperLog 各表 dept/user 字段
3. 8 个集成测试 (DataScopeServiceTest) 验证 list 受限
4. 业务方通知: 升级后 list 数量减少属于正常

### PR3: 跨模块 DataScopeProvider 桩 (缺口 #18)

| 项 | 内容 |
|----|------|
| 涉及缺口 | #18 (跨模块 Provider 缺位) |
| 工作量 | 1-2 天 |
| 改动文件 | platform-ops / platform-workflow / platform-message 各加 1 个 Provider 桩 |
| 风险 | 中 (多模块改动, 启动时可能有 Bean 冲突) |
| 验证 TC | 3 个 (每模块 1 个 TC, 启动验证 Provider 不为 null) |
| 灰度 | 桩实现加 `@ConditionalOnMissingBean(DataScopeProvider.class)`, 避免多 Provider 冲突 |

**实施步骤**:
1. `OpsDataScopeProviderImpl` (platform-ops): 默认返回 `DataScopeContext.none()`
2. `WorkflowDataScopeProviderImpl` (platform-workflow): 同上
3. `MessageDataScopeProviderImpl` (platform-message): 同上
4. 3 个模块的 application.yml 启用 (默认就启用, 不需 yml 改动)
5. 启动 3 个模块验证: Provider bean 存在, Aspect 不退化

### PR4: UPDATE/DELETE 拦截器扩展 + fail-closed (缺口 #15 + #16)

| 项 | 内容 |
|----|------|
| 涉及缺口 | #15 (写操作零防护) + #16 (SQL 解析失败静默越权) |
| 工作量 | 1 周 (写操作扩展 5 天 + fail-closed 1 天 + TC 1 天) |
| 改动文件 | `DataScopeInnerInterceptor.java` (核心改写逻辑) |
| 风险 | 高 (写操作 + 业务中断风险) |
| 验证 TC | 5 + 4 = 9 个 (写操作 5 个 + 复杂 SQL 4 个) |
| 灰度 | 同上 |

**实施步骤**:
1. `DataScopeInnerInterceptor` 新增 Update/Delete 分支 (instanceof 检查)
2. 改写逻辑: 找到 WHERE 子句, 末尾追加 AND fragment
3. **fail-closed**: `JSQLParserException` → 改 `log.warn + 放行` 为 `log.error + 抛 BizException`
4. **依赖**: 必须与 #17 (PR2) 一起, 否则 @DataScope 注解没业务应用, 拦截器改写无意义
5. **强制**先 PR1-3 全部部署后再开 PR4 的灰度开关

### PR 顺序与并行

```
PR1 (1d) ──→ PR2 (1-2d) ──┐
                          ├──→ PR4 (1w, 风险高, 最后)
                PR3 (1-2d) ──┘

PR2 与 PR3 可并行 (不同模块, 无冲突)
PR4 必须在 PR1-3 全部部署后再启动 (有依赖)
```

### PR1 实施完成 (2026-06-05) ✅

| 项 | 内容 |
|----|------|
| 分支 | `feat/m5-p0-2-pr1-cte` |
| Commits | e64e3f7 (代码) + 4a86811 (#15 入档) + 4838efd (M7 收尾) |
| 单元测试 | 8/8 PASS (UserDataScopeProviderImplTest, 新增 TC-DS-06/07/08) |
| 集成测试 | 3/3 PASS (DataScopeIntegrationTest baseline) |
| CI 验证 | 28/28 PASS (check-data-scope-upgrade-toggle.sh) |
| 真 MySQL 8 端到端 | 4/4 PASS (doc/PR1-m3-cte-verification.sql: 顶级/子/孙/无子 + 软删除) |
| 部署 | platform-user healthy, admin 登录 200 + /user/list 200 OK (灰度 false + true 两态) |
| Flyway 修复 | KNOWN_ISSUES #14 ✅ DROP DATABASE platform + 重建 + 18/18 success=1 |
| 文档 | KNOWN_ISSUES #15 ✅ (Flyway 启动失败已解决) + #19 🟡 P2+ (跨 org 性能, 非安全) |
| 决策 | v1.1.1 缺口 #19 P0→P2+ 降级 (sys_dept 无 tenant_id 字段, dept 跨 org 是业务设计) |

### 启动 checklist (每个 PR 通用)

```
- [ ] git checkout -b feat/m5-p0-2-prN-<scope>
- [ ] 改动文件 + TC
- [ ] mvn -pl <module> test (单测)
- [ ] docker compose up -d <module> (集成测试)
- [ ] 业务接口验证: 灰度开关 true 时新行为生效
- [ ] git push → CI (5-10 min)
- [ ] docker compose pull && docker compose up -d
- [ ] 灰度开关 false 状态下回退验证 (确保灰度可控)
- [ ] commit + push + 通知业务方
- [ ] KNOWN_ISSUES.md 标记对应缺口为 🟢 已解决
```

## 九、PR2 启动准备 (2026-06-05 起草, 2026-06-05 复核 ⚠️ 不可行)

### 9.0 复核结论 (2026-06-05)

**原计划**: 8 Service 加 @DataScope 注解 (Role/Dept/Menu/Post/Org/Dict/OperLog/TenantApp)

**复核结果**: 8 Service 中**0 个可直接加注解**, 需先**数据模型扩展 + 重新设计**, 详见下表:

| Service | 实体字段 | 注解可加性 | 原因 |
|---------|---------|-----------|------|
| Role | 无 deptId/createBy | ❌ SQL 报错 | 拦截器加 `AND role.dept_id IN (...)`, sys_role 表**无 dept_id 列** |
| Dept | id (主键) | ⚠️ 破坏语义 | 现有 `listByOrgId(Long orgId)` / `listByDeptId(Long deptId)` 已被入参过滤, 加注解后用户权限范围外的 dept 会被剔除, 但入参 deptId **可能超出用户范围** |
| Post | 有 deptId | ⚠️ 破坏语义 | 同 Dept, `listByDeptId(Long deptId)` 已被入参过滤 |
| Organization | 无 deptId (有 parentId) | ❌ SQL 报错 | sys_org 表**无 dept_id 列** |
| OperLog | 无 deptId/userId, 有 operName | ❌ SQL 报错 | sys_oper_log 表**无 dept_id / user_id 列** |
| Menu | 无 deptId (菜单全局) | ❌ 不应过滤 | 菜单是 RBAC 权限对象, 不应按 data_scope 过滤 |
| Dict | 无 deptId (字典全局) | ❌ 不应过滤 | 字典是全局静态数据, 不应按 data_scope 过滤 |
| TenantApp | tenantId (走 P0-1) | ❌ 应走多租户 | 租户应用应走多租户拦截器 (P0-1) 而非 data_scope |

**修正方案** (3 选 1):
- **方案 A** (推荐): PR2.0 数据模型扩展 — Flyway V23 给 `sys_role` / `sys_org` / `sys_oper_log` 加 `dept_id` + `create_by` 字段 (V22 已用 BaseEntity.createBy, 部分表已存在), 然后 PR2.1 实施 3 Service 注解 (Role/Organization/OperLog)
- **方案 B**: 跳过 PR2, 直接进 PR3 (跨模块 Provider 桩) — PR2 留待 M5.5 数据模型扩展一并解决
- **方案 C**: PR2 缩到 Controller 层调用 — 加注解在 UserController.pageUser / RoleController.listRole 等 Controller, 不在 ServiceImpl

**用户决策 (待确认)**: 9.4 → 9.5 拍板
- A 方案: 数据模型扩展 + 3 Service 注解 (1-2 天)
- B 方案: 跳过 PR2, 立即 PR3 (1-2 天)
- C 方案: Controller 层注解 (1-2 天)

### 9.1 目标 (原计划, 现作废)

业务层 8 个 Service.list/page 方法补 `@DataScope(deptAlias=..., userAlias=...)` 注解, 让 MyBatis-Plus 拦截器在 SQL WHERE 末尾追加 dept/user 权限条件 (KNOWN_ISSUES #17)。

### 9.1 目标

业务层 8 个 Service.list/page 方法补 `@DataScope(deptAlias=..., userAlias=...)` 注解, 让 MyBatis-Plus 拦截器在 SQL WHERE 末尾追加 dept/user 权限条件 (KNOWN_ISSUES #17)。

**8 个 Service** (均在 platform-user module):
1. `RoleServiceImpl.list` → `sys_role.dept_id` (dept 树)
2. `DeptServiceImpl.list` → `sys_dept.dept_id` (dept 树)
3. `MenuServiceImpl.list` → 无 dept_id (菜单不过滤, **不**加注解)
4. `PostServiceImpl.list` → `sys_post.dept_id` (dept 树)
5. `OrgServiceImpl.list` → `sys_org.dept_id` (dept 树)
6. `DictServiceImpl.list` → 无 dept_id (字典全局, **不**加注解)
7. `OperLogServiceImpl.list` → `sys_oper_log.oper_user_id` (user 维度)
8. `TenantAppServiceImpl.list` → `sys_tenant_app.tenant_id` (tenant 维度, **走 P0-1 拦截器, 不加注解**)

**实际加注解数量**: 5 个 (Role/Dept/Post/Org/OperLog), 跳过 3 个 (Menu/Dict 无需, TenantApp 走 P0-1)

### 9.2 H2 mock 桩 + SQL 模板

8 个 Service 的 mapper.xml 已有标准 SELECT, 拦截器会在 WHERE 末尾追加 `AND <deptAlias>.dept_id IN (1,2,3)`, **不需改 SQL**, 只需加注解。

**集成测试 TC-DS-09 ~ TC-DS-13** (5 个):
- 准备数据: admin (scope=1) / zhangs (scope=5 自定义 dept) / liqi (scope=3 本部门)
- 验证: scope=5 调用 `RoleService.list` 返回 zhangs 自定义 dept 内的 role
- 验证: scope=3 调用 `DeptService.list` 返回本部门 + 子部门
- 验证: scope=3 调用 `PostService.list` 返回本部门 + 子部门 post
- 验证: scope=1 (admin) 调用 `OrgService.list` 返回全量
- 验证: scope=3 调用 `OperLogService.list` 返回本部门 user 的操作日志

**测试方式**: 复用 `DataScopeIntegrationTest` 的 H2 桩 (MyBatis 拦截器集成), 8 个测试方法

### 9.3 灰度开关

**复用 PR1 灰度开关** `platform.data-scope.upgrade.enabled=true`:
- 启用: 拦截器处理 @DataScope 注解, 5 个 Service 实际受限
- 关闭: 拦截器 skip @DataScope, 5 个 Service 走原 SQL (全量)

**配置**: 5 个 yml 已加 (PR1 同步), **PR2 不需新增 yml 配置**。

### 9.4 commit message 模板

```bash
git add code/platform-server/platform-user/src/main/java/com/cloudhub/platform/user/service/impl/ \
        code/platform-server/platform-user/src/test/java/com/cloudhub/platform/user/tenant/

git commit -m "feat(m5): PR2 业务层 5 个 Service 补 @DataScope 注解 (缺口 #17)

- RoleServiceImpl.list: deptAlias=role, userAlias=createBy
- DeptServiceImpl.list: deptAlias=dept, userAlias=createBy
- PostServiceImpl.list: deptAlias=post, userAlias=createBy
- OrgServiceImpl.list: deptAlias=org, userAlias=createBy
- OperLogServiceImpl.list: userAlias=operUserId
- 5 个集成测试 (DataScopeServiceTest) 验证 list 受限
- 灰度: platform.data-scope.upgrade.enabled=true"
```

### 9.5 启动 checklist (PR2)

```
- [ ] 拍板 PR2 启动
- [ ] git checkout -b feat/m5-p0-2-pr2-datascope-services
- [ ] 核查 5 个 Service.list/page 字段 (deptAlias/userAlias)
- [ ] 加 @DataScope 注解 (5 个 Service)
- [ ] 写 5 个集成测试 (DataScopeServiceTest, TC-DS-09~13)
- [ ] mvn -pl platform-user test (单测 + 集成测试)
- [ ] 业务接口验证 (灰度 false / true 两态)
- [ ] commit + push → CI 5-10 min
- [ ] docker compose pull && docker compose up -d platform-user
- [ ] 验证 admin 登录 200 + 各 Service.list 受限
- [ ] KNOWN_ISSUES #17 标 🟢 已解决
- [ ] 拍板 PR3 启动
```

---

## 十、PR3 启动准备 (2026-06-05 起草, 待用户拍板, 与 PR2 并行)

### 10.1 目标

3 个跨模块 DataScopeProvider 桩实现, 让 platform-ops / platform-workflow / platform-message 启动时不为 Aspect 报 "no DataScopeProvider bean" 错误 (KNOWN_ISSUES #18)。

**3 个 Provider 桩**:
1. `OpsDataScopeProviderImpl` (platform-ops): 默认返回 `DataScopeContext.none()`, `@ConditionalOnMissingBean(DataScopeProvider.class)` 防止多 Provider 冲突
2. `WorkflowDataScopeProviderImpl` (platform-workflow): 同上
3. `MessageDataScopeProviderImpl` (platform-message): 同上

### 10.2 实施步骤

1. 在 `platform-common` 加 `DataScopeProvider` 接口 (供各模块实现, 类似 TenantLineHandler)
2. 3 个模块各加 1 个 Provider 桩 (extends `AbstractDataScopeProvider` 或 implements `DataScopeProvider`)
3. 3 个 yml 默认启用 (`spring.beans.factory.data-scope-provider=stub`)
4. 启动 3 个模块验证: Provider bean 存在, Aspect 启动不报缺 bean

### 10.3 集成测试

- `OpsDataScopeProviderTest` (platform-ops): 验证 Provider bean 注入, Aspect 调用不空
- `WorkflowDataScopeProviderTest` (platform-workflow): 同上
- `MessageDataScopeProviderTest` (platform-message): 同上

**总 TC**: 3 个, 每个 1 测试方法 (启动 + 调 Aspect.getDataScope)

### 10.4 灰度开关

3 个模块 yml 加 `platform.data-scope.cross-module-provider=enabled` (默认 enabled), **不需新增 yml 配置** (复用 PR1 开关)。

### 10.5 commit message 模板

```bash
git add code/platform-server/platform-common/src/main/java/com/cloudhub/platform/common/datascope/ \
        code/platform-server/platform-ops/src/main/java/com/cloudhub/platform/ops/datascope/ \
        code/platform-server/platform-workflow/src/main/java/com/cloudhub/platform/workflow/datascope/ \
        code/platform-server/platform-message/src/main/java/com/cloudhub/platform/message/datascope/ \
        code/platform-server/platform-ops/src/test/ \
        code/platform-server/platform-workflow/src/test/ \
        code/platform-server/platform-message/src/test/

git commit -m "feat(m5): PR3 跨模块 DataScopeProvider 桩 3 个 (缺口 #18)

- OpsDataScopeProviderImpl (platform-ops): 默认返回 DataScopeContext.none()
- WorkflowDataScopeProviderImpl (platform-workflow): 同上
- MessageDataScopeProviderImpl (platform-message): 同上
- 3 个集成测试 (启动 + 调 Aspect.getDataScope)
- @ConditionalOnMissingBean 防止多 Provider 冲突"
```

### 10.6 启动 checklist (PR3)

```
- [ ] 拍板 PR3 启动 (可与 PR2 并行)
- [ ] git checkout -b feat/m5-p0-2-pr3-provider-stubs
- [ ] platform-common 加 DataScopeProvider 接口 + AbstractDataScopeProvider 抽象类
- [ ] 3 个 Provider 桩 (Ops/Workflow/Message)
- [ ] 3 个 yml 启用 (默认 enabled)
- [ ] 3 个集成测试 (TC-DS-14/15/16)
- [ ] mvn 编译 3 模块
- [ ] docker compose up -d platform-ops workflow message
- [ ] 验证 3 服务 healthy + Provider bean 存在
- [ ] 业务接口验证: ops / workflow / message 启动 200
- [ ] commit + push → CI 5-10 min
- [ ] docker compose pull && docker compose up -d
- [ ] KNOWN_ISSUES #18 标 🟢 已解决
- [ ] 拍板 PR4 启动 (待 PR1-3 部署完)
```

---

## 十一、PR4 启动准备 (2026-07 之后, 待 PR1-3 部署完)

**触发条件**: PR1+PR2+PR3 全部部署完, 灰度开关可独立切到 PR4。

**目标**: UPDATE/DELETE 拦截器扩展 + fail-closed (KNOWN_ISSUES #15 + #16)。

**预计**: 1 周 (高风险, 最后)

**详见 §八 PR4 描述 (320-336 行)。**

---

## 十二、相关 commit

| Commit | 分支 | 内容 |
|--------|------|------|
| b5823c4 | feat/m5-p0-2-pr1-cte | docs(m5): PR1 准备文档 (H2 mock 桩 + 灰度开关 yml + commit 模板) |
| e64e3f7 | feat/m5-p0-2-pr1-cte | fix(m5): PR1 #19 dept 树改 MySQL 8 递归 CTE (灰度开关, 老 DFS 保留 fallback) |
| 4a86811 | feat/m5-p0-2-pr1-cte | docs(known-issues): register #15 PR1 部署遇 Flyway 启动失败 (临时绕过) |
| 4838efd | feat/m5-p0-2-pr1-cte | docs(m5-pr1): 标记 #14 #15 已解决 + v7.2 PR1 部署完成 + Flyway 修复报告 |
| 71a68bb | feat/m5-p0-2-pr1-cte | docs(m5-plan): PR2 + PR3 启动准备 + PR1 实施结果 + commit 列表 |
| 1c35435 | feat/m5-p0-2-pr2-datascope-services | docs(m5-pr2): 复核 PR2 不可行 - 8 Service 中 0 个可直接加 @DataScope (3 修正方案待拍板) |
| da86a3a | feat/m5-p0-2-pr2-datascope-services | feat(m5): PR2 业务层 2 Service 加 @DataScope 注解 (3 方法, 36/36 TC PASS) |
| b8f535e | feat/m5-p0-2-pr2-datascope-services | feat(m5): PR3 跨模块 DataScopeProvider 桩 3 个 (3/3 TC PASS) |
| (待 PR4) | feat/m5-p0-2-pr4-write-strict | UPDATE/DELETE 拦截器 + fail-closed |

### PR2 实施详情 (2026-06-05, 缩窄 3 方法)

**原计划**: 8 Service 加 @DataScope
**实际可加**: 2 Service + 3 方法 (用户拍板 缩窄方案, Menu/Dict/TenantApp 跳过)
- **PostServiceImpl.listByOrgId** `@DataScope(deptAlias="dept_id")` ✅
- **DeptServiceImpl.listByOrgId** `@DataScope(deptAlias="id")` (主键, sys_dept 无 dept_id 列) ✅
- **DeptServiceImpl.listTreeByOrgId** `@DataScope(deptAlias="id")` ✅
- **未加**: Menu/Dict/TenantApp (全局/走 P0-1, 用户确认)
- **未加**: listByDeptId (入参已过滤, 加注解破坏语义)
- **未加**: Role/Org/OperLog (实体无 deptId, 留 M5.5 数据模型扩展)

**关键发现**: @DataScope 必须加在 **Impl 类方法** (CGLIB 代理可识别), 不能加接口方法 (JDK 代理扫描不到)
- 加在 PostService 接口 → 36/36 FAIL
- 加在 PostServiceImpl 类 → 36/36 PASS

**测试覆盖**: 3 TC (DataScopeServiceTest) + 全套 36/36 PASS

### PR3 实施详情 (2026-06-05)

**3 个 Provider 桩** + `@ConditionalOnMissingBean(DataScopeProvider.class)` 防止多 Provider 冲突
- **OpsDataScopeProviderImpl** (platform-ops) — 桩返回 none()
- **WorkflowDataScopeProviderImpl** (platform-workflow) — 桩返回 none()
- **MessageDataScopeProviderImpl** (platform-message) — 桩返回 none()

**测试覆盖**: 3 TC (纯单元测试, 不启 Spring) — TC-DS-14/15/16 全 PASS
