# Handoff: 2026-06-04 → 2026-06-05

> 本 handoff **优先反映事实**。今日 19:00 之前的内容已合入；新增章节 **§今日下午 (Flyway + KNOWN_ISSUES)** 见后段。

---

## 今日产出总结

### 4 个 Git 提交 (上午段, 已 push)

```
5fd22c5 feat(docker): docker-compose.prod.yml 模板 (4C8G/8C16G)
ff087d9 docs(p0-1): 回滚开关设计 - ConditionalOnProperty 方案 A
1439e72 docs(p0-1): 集成测试 checklist - 8 个 TC + SQL 审计脚本
c85cb6f docs(p0-1): M4 完成度审计 - 对齐主规划 §5.1 与实际代码状态
```

### 3 个 Git 提交 (下午段, 已 push)

```
#1  fix(logstash): remove non-existent gelf codec + add KNOWN_ISSUES
    - docker/logstash/logstash.conf (删 codec => gelf)
    - doc/KNOWN_ISSUES.md (新增 13 条)

#2  chore: enable Flyway, persist MySQL init.sql, update README
    - .gitignore (精确排除)
    - docker/mysql/{init.sql, my.cnf} (进 git)
    - docker-compose.yml (删 4 处 SPRING_FLYWAY_ENABLED=false)
    - README.md (路线图状态)
    - doc/KNOWN_ISSUES.md (#8 解决)

#3  docs(known-issues): register #14 明日执行 Flyway 重建步骤
    - doc/KNOWN_ISSUES.md (#11/#13 索引 + #14 完整步骤)
```

### 文档产出

| # | 文件 | 行数 | 角色 |
|---|------|------|------|
| 1 | `doc/P0-1-M4-完成度审计.md` | 300 | P0-1 现状正式入档 (上午) |
| 2 | `doc/P0-1-集成测试checklist.md` | 345 | 8 个 TC + SQL 审计脚本 (上午) |
| 3 | `doc/P0-1-回滚开关设计.md` | 399 | 3 方案对比 + 推荐 A (上午) |
| 4 | `docker-compose.prod.yml` | 365 | 生产环境 override 模板 (上午) |
| 5 | `doc/KNOWN_ISSUES.md` | 650+ | 14 条问题/决策/教训 (下午) |

### 代码改动 (下午)

| 文件 | 改动 | 原因 |
|------|------|------|
| `docker/logstash/logstash.conf` | 删 `codec => gelf` | 非 OSS 插件, gelf input 自动处理 |
| `docker-compose.yml` | 删 4 处 `SPRING_FLYWAY_ENABLED=false` | 用户决策: 用 Flyway 自动管 |
| `.gitignore` | `docker/mysql/` → `docker/mysql/data/` | init.sql/my.cnf 需进 git |
| `docker/mysql/init.sql` | 加 GRANT + platform_nacos 库 | 修 platform-message 500 根因 |
| `README.md` | 路线图状态 M4=✅ M5=🟡 | 状态过期 |

### Git 状态 (会话结束 19:00)

```
分支: develop
本地: 0 commit ahead of origin/develop
CI: 触发中 (Flyway=true 改动 → 重新构建镜像, 5-10 min)
```

---

## 今日下午关键决策 (用户拍板)

| 决策 | 选 | 理由 |
|------|---|------|
| Flyway 开关设计 | C (删 false, 自动管) | "开关是为了以后不影响生产环境" |
| .gitignore 修复 | 1 (精确排除子目录) | "需要提交的都使用 flyway 方式初始化的脚本" |
| M5 跨服务 TC | 暂不修进 backlog | 当前业务不触发跨服务上下文 |
| xxl-job unhealthy | 先忽略 | 业务可调 |
| README 文档同步 | A (直接改) | 简单, 改几行 |

---

## ⚠️ 待明日执行 (P0 阻塞)

详见 `doc/KNOWN_ISSUES.md #14` 完整步骤:

1. **启动 Docker Desktop** (用户已主动关闭, 19:00 状态)
2. **等 CI 镜像构建完** (5-10 min, 200MB/服务)
3. **DROP DATABASE platform_message** (清掉临时建的 4 张表)
4. **`docker compose pull && docker compose up -d`** (触发 Flyway 跑 V1)
5. **验证 4 张表自动建立 + 业务接口 200**
6. **更新 KNOWN_ISSUES 状态** (#11/#14 标已解决)
7. (可选) 写本 handoff 的 v1.1 完结版

**风险**:
- DROP 库会清掉 10 条 sys_message 种子数据 (临时建的, 没价值, 可选备份)
- Flyway 失败时回滚: 加回 `SPRING_FLYWAY_ENABLED=false` 重启

---

## 重要文件位置

- 当前主规划: `doc/中台建设中长期规划.md` (v1.0)
- 历史规划: `doc/opencode实施计划.md` (v7.0)
- 操作日志 bug 修复: 之前已完成 (OperLogAspect + 6 个 @Log)
- 多租户: `code/platform-server/platform-common/.../MybatisPlusConfig.java` (9999 → null 模式)
- 数据权限: `code/platform-server/platform-common/.../DataScope*.java` (5 文件, 决策 1/2/3 已实施)

---

## 用户强约束 (必须遵守)

1. **永远不要在 Docker Desktop 上构建镜像** ✅ 全程遵守
2. **不要本地推镜像到 Docker** ✅ 全程遵守
3. **代码改动通过 git push → CI → 拉镜像部署** ✅ 今日 3 次 push 走完整流程
4. **所有变更需要先做规划, 文档记录后再说** ✅ KNOWN_ISSUES.md 是这个原则的产物
5. 文档结构: `doc/` 目录, 按 `*.md` 格式 ✅

---

## 用户偏好 (从对话学习)

- **风格**: 详细、谨慎、可回滚
- **关注点**: 可用性 > 可扩展性 > 性能
- **决策方式**: 喜欢"先分析合理性 + 给出决策 + 解释理由"
- **文档**: 喜欢结构化表格、决策日志、思考过程
- **强烈偏好**: 用 KNOWN_ISSUES.md 统一管理问题
- **重要决策点用词**: plan / 拍板 / 决策
- **修问题方式**: 喜欢 B 方案 (最小持久化) > 推到 backlog

---

## 临时文件 (不提交, 工作区遗留)

```
docker-compose.override.yml   # 临时
logs_user.json                # 测试日志
test-candidate.java           # 多租户测试
test-extract.js               # BpmnDesigner 测试
test-inject.js                # BpmnDesigner 测试
doc/mysql-schema.sql          # 用途不明, 需用户确认
```

**用户未确认**这些是否有用, **不擅自处理**。明日可以问一句"这几个文件还要吗?"

---

## 与昨日 handoff 的关系

`HANDOFF_2026-06-04.md` **保留作历史档案** (不再更新)。

事实以本 handoff + 5 份新文档 + KNOWN_ISSUES.md 为准。

---

## 变更历史

| 版本 | 日期 | 变更 |
|------|------|------|
| v1.0 | 2026-06-04 上午 | 初版: 4 commit + 4 份新文档 + Flyway=false 决策 |
| v1.1 | 2026-06-04 19:00 | 合并下午段: 3 commit + KNOWN_ISSUES.md + Flyway=true 决策 + 明日必做 |

---

## 明日 (2026-06-05) 启动检查清单

```powershell
# 1. 启动 Docker Desktop
# 2. 打开 GitHub Actions 页面, 确认 CI 绿
# 3. 跑 KNOWN_ISSUES.md #14 的 7 步执行
# 4. 验证完成后, 改本 handoff 为 v1.2 完结版
# 5. 清理临时文件 (可选)
```
