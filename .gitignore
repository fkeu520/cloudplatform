# IDE
.idea/
.vscode/
*.iml
# Eclipse
.classpath
.factorypath
.project
.settings/

# Maven
target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties

# Node
node_modules/
dist/
.npm

# Logs
logs/
*.log

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.local
*.local

# Docker
# 2026-06-04 修复: 不要整个目录排除, 允许 .sql/.cnf/.conf 等配置进 git
# 只忽略未来的 data 目录 (docker volume 挂载点) / minio 数据卷
docker/mysql/data/
docker/minio/data/

# Build
*.class
*.jar
*.war

# Temp
*.tmp
*.bak
*.swp
tmp_*
test-candidate.java
test-extract.js
test-inject.js

# Logs / local data
logs_user.json
backup_platform_*.sql

# Docs (WIP restructuring, 2026-06-16 重新设计)
# 重组原则: doc/log / doc/decision / doc/handoff 进 git (项目状态/决策/会话交接)
#           doc/ops / doc/plan / doc/spec / doc/sql 不进 git (运维手册/中期规划/规范/SQL 是参考文档)
# 注: phase_-1-*.md 是 csyh 融合 W1 产出物, 默认进 doc/log/, 无需额外放行
doc/ops/
doc/plan/
doc/spec/
doc/sql/

# Codegraph / tool artifacts
.codegraph/
.playwright-mcp/
.opencode/          # opencode agent session artifacts (commands, skills, state)
openspec/           # openspec workflow artifacts
openflow/           # openflow workflow artifacts

# Python
*.pyc
__pycache__/

# Batch
启动*.bat
检查环境.bat
HANDOFF_*.md

# Internal protocol docs (含 SSH/部署信息, 不进 git)
docs/handoff/git-deploy-workflow-protocol.md
